user www-data;
worker_processes auto;

events {
	worker_connections 1024;
}

http {
    include /etc/nginx/conf.d/*.conf;
    default_type application/octet-stream; #TODO it was in zackiewiczs nginx.conf, not sure if needed
}